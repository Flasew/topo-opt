import regl from 'regl';
import { NumericAccessor } from "../../config";
import { GraphData } from "../GraphData";
import { InputNode, InputLink } from "../../types";
export declare function createSizeBuffer<N extends InputNode, L extends InputLink>(data: GraphData<N, L>, reglInstance: regl.Regl, pointTextureSize: number, sizeAccessor: NumericAccessor<N>): regl.Framebuffer2D;
